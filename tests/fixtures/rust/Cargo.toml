[package]
name = "my-rust-app"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
tokio = { version = "1.28", features = ["full"] }
reqwest = "0.11"
anyhow = "1.0"

[dev-dependencies]
pretty_assertions = "1.3"

[build-dependencies]
cc = "1.0"
